{
  "name": "maplemail-bot",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node bot.js",
    "postinstall": "npx puppeteer browsers install chrome --path $HOME/.cache/puppeteer && cp -r $HOME/.cache/puppeteer /opt/render/project/.puppeteer-cache"
  },
  "dependencies": {
    "express": "^4.18.2",
    "whatsapp-web.js": "^1.23.0",
    "qrcode": "^1.5.3",
    "puppeteer": "^22.0.0"
  }
}